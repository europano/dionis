<form method="post" action="{{ path('document_delete', {'id': document.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
<div>
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document.id) }}">
    </div>
    <button class="btn">Delete</button>
    <a href="{{path('admin')}}" class="btn btn-primary"> retour vers l'accueil </a> 
<a href="{{path('document_index')}}" class="btn btn-primary"> retour vers la liste</a> 
</form>
