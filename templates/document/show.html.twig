{% extends 'base.html.twig' %}

{% block title %}document{% endblock %}

{% block body %}
    <h1>Liste des documents</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('document_index') }}"retour vers la liste</a>

    {{ include('document/_delete_form.html.twig') }}
      <a href="{{ path('admin') }}"retour vers l'accueil</a>
{% endblock %}
